import 'package:flutter/material.dart';

class Ingridiend {
  String? name;
  Icon? icon;
  String? scale;

  Ingridiend({
    @required this.name,
    @required this.icon,
    @required this.scale,
  });
}
